name: c2btechfinaltechnicaldemo
description: A new Flutter application.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  #  maps package
  google_maps_flutter: ^0.5.28+1
  #  Current location
  geolocator: ^5.3.1
  #  Network calls - get request
  http: ^0.12.1
  provider: ^4.1.2



dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
